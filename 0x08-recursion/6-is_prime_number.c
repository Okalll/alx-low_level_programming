#include "main.h"
/**
 * is_prime_number - finds prime numbers among integers
 * @n: the integer
 * Returns: 0 or 1
 */
int is_prime_number(int n)
{
	if (n = prime number)
		return (1);
	else
		return (0);
}
